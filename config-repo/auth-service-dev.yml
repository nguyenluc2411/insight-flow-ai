# =============================================================================
# auth-service-dev.yml — Dev profile overrides for auth-service
# =============================================================================

logging:
  level:
    com.insightflow.auth: DEBUG
    org.springframework.security: DEBUG
    org.flywaydb: DEBUG
