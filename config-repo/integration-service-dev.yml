# =============================================================================
# integration-service-dev.yml — Dev profile overrides for integration-service
# =============================================================================

logging:
  level:
    com.insightflow.integration: DEBUG
    org.springframework.kafka: DEBUG
    org.flywaydb: DEBUG
